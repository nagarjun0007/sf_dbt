sources:
  - name: "nag_sources"
    database: VOYA
    schema: BRONZE
    tables:
      - name: USERS


models:
  - name: users
    config:
      materialized: 'table'
  - name: sahil
    config:
      materialized: 'view'
  - name: nag_city
    config:
      materialized: 'table'


